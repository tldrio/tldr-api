<div class="row">
  <div class="tldr-base-unit page-tldr-container">
    <div class="tldr-read-container">
      <div class="tldr-tldr">
        <div class="tldr-header">
          <img class="tldr-favicon tldr-favicon-ellipsis" src="http://www.google.com/s2/favicons?domain={{tldr.hostname}}" alt="Favicon for {{tldr.hostname}}">
          <h1 class="tldr-title tldr-page-title"><a target="_blank" href="{{tldr.originalUrl}}">{{tldr.title}}</a></h1>
          <span class="tldr-hostname">({{tldr.hostname}})</span>
        </div>

        <div class="tldr-metadata">
          <span>
            tl;dr by
              <span><a href="/{{tldr.creator.username}}" target="_blank"><em>{{tldr.creator.username}}</em></a></span>
              - read {{tldr.readCount}} times
          </span>
        </div>

        {{#tldr.imageUrl}}
        <div class="tldr-image">
          <img src="{{tldr.imageUrl}}" alt="{{tldr.title}}">
        </div>
        {{/tldr.imageUrl}}

        <div class="tldr-summary">
          <ul>
            {{#tldr.summaryBullets}}
            <li class="tldr-bullet">
              <i class="icon-stop"></i>
                    {{.}}
            </li>
            {{/tldr.summaryBullets}}
          </ul>
        </div>

        <!-- just in case the image is taller than the summary -->
        {{#tldr.imageUrl}}
        <div class="clearfix"> </div>
        {{/tldr.imageUrl}}
      </div>

      <div class="tldr-tldr">
        <div class="sub-tldr">
          <a href="{{tldr.originalUrl}}" target="_blank" id="read-full-article" class="btn btn-link">Read full article</a>
          <div class="sharing-container">
            <div class="btn-group dropup">
              <button class="btn btn-link dropdown-toggle" data-toggle="dropdown">
                Share tl;dr
              </button>
            </div>
            <div class="btn-group dropup">
              <button class="btn btn-link dropdown-toggle" data-toggle="dropdown">
                Save full article for later
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="page-help">
  <div class="row">
    <div class="">
      <span class="help-inline">This is a summarized version.</span>
      <a id="cta-to-tldr" href="http://tldr.io/whatisit" target="_blank"><small>Where does this summary come from?</small></a>
      {{#loggedUser.isAdmin}}
      <br><a href="{{dbguiUrl}}/tldrs/{{tldr._id}}/edit"><i class="icon icon-edit"></i>admin in db</a>
      {{/loggedUser.isAdmin}}
    </div>
  </div>
</div>
